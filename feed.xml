<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://yuntaodu.me/feed.xml" rel="self" type="application/atom+xml" /><link href="https://yuntaodu.me/" rel="alternate" type="text/html" hreflang="en" /><updated>2026-04-12T15:48:26+00:00</updated><id>https://yuntaodu.me/feed.xml</id><title type="html">Yuntao Du @ Purdue</title><subtitle>A research website of Yuntao Du. Based on [*folio](https://github.com/bogoli/-folio) design.
</subtitle><entry><title type="html">Imitation, Not Shadowing</title><link href="https://yuntaodu.me/blog/2026/IMIA/" rel="alternate" type="text/html" title="Imitation, Not Shadowing" /><published>2026-02-03T00:00:00+00:00</published><updated>2026-02-03T00:00:00+00:00</updated><id>https://yuntaodu.me/blog/2026/IMIA</id><content type="html" xml:base="https://yuntaodu.me/blog/2026/IMIA/"><![CDATA[<p>This is the gentle introduction to our <a href="https://arxiv.org/abs/2509.06796">work</a>, which will be presented at <a href="https://www.usenix.org/conference/usenixsecurity26">USENIX Security 2026</a>. Code and datasets are available on <a href="https://github.com/zealscott/IMIA">Github</a>.</p>

<h2 id="introduction">Introduction</h2>

<p>Membership Inference Attacks (MIAs) have become the standard tool for measuring data leakage in machine learning models. The goal is simple: given a data point and a trained model, determine whether that data point was used during training.</p>

<p>Over the last serval years, the research community has viewed shadow training<d-cite key="sp17membership"></d-cite> as the gold standard for achieving highly powerful MIAs. In fact, all state-of-the-art MIAs utilize shadow training techniques. The intuition is simple: to understand how the target model behaves on members and non-members, the adversary trains multiple “shadow models” on similar data distributions. By observing how these shadow models behave on their own training data (members) versus unseen data (non-members), the adversary learns the behavioral patterns of membership and uses this knowledge to attack the target model.</p>

<p>However, state-of-the-art shadow-based attacks such as LiRA<d-cite key="sp22lira"></d-cite> and PMIA<d-cite key="ndss26cpmia"></d-cite> come with a significant computational cost. To obtain reliable estimates of member and non-member behavior, these methods often require training hundreds of shadow models. This can consume hundreds of GPU hours and has been criticized as impractical, even for privacy auditing.</p>

<p>In this work, we asked a fundamental question: <em>Is this massive computational cost of shadow training really necessary?</em></p>

<h2 id="the-problem-with-target-agnostic-shadowing">The Problem with Target-Agnostic Shadowing</h2>

<p>We identify that much of the inefficiency in existing methods comes from their <strong>target-agnostic</strong> design.
Standard shadow models are trained to learn general behavioral distributions, but they are not trained to learn the specific idiosyncrasies of the target model under attack. 
As a result, shadow models exhibit high predictive variance. 
As shown in the top row of the figure below, the score distributions for members and non-members derived from shadow models often overlap significantly, especially for “hard” instances. This variance forces attackers to train hundreds of models just to statistically average out the noise.</p>

<div class="row justify-content-md-center">
    <div class="col-sm mt-0 pb-0 mt-md-0">
 <figure>

  <picture>
    
    <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/2026-02-03-IMIA/scores-480.webp" />
    <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/2026-02-03-IMIA/scores-800.webp" />
    <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/2026-02-03-IMIA/scores-1400.webp" />
    

    <!-- Fallback to the original file -->
    <img src="/assets/img/2026-02-03-IMIA/scores.png" class="img-fluid rounded z-depth-1" width="30%" height="auto" title="Comparing score distributions. Top: Target-agnostic shadow models show high variance and overlap. Bottom: Our target-informed imitative models produce tight, separable distributions." onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />

  </picture>

</figure>

    </div>
</div>
<div class="caption">
 Comparing score distributions. Top: Target-agnostic shadow models show high variance and overlap. Bottom: Our target-informed imitative models produce tight, separable distributions.
</div>

<h2 id="our-solution-imitative-training">Our Solution: Imitative Training</h2>

<p>To address this, we introduce the Imitative Membership Inference Attack (IMIA). Instead of training shadow models that are indepenedent from the target model, we explicitly train imitative models to <em>mimic</em> the target.</p>

<p>IMIA operates in two stages to capture the behavioral discrepancy between members and non-members:</p>
<ol>
  <li>Imitative OUT Models: We first train a model to match the target’s output on a reference dataset. Since the target model didn’t see this reference data during its training, this imitative model learns to replicate how the target behaves on non-members.</li>
  <li>Imitative IN Models: We then fine-tune this model using a small set of “pivot” instances. These pivots act as proxies for members, allowing the model to capture how behavior changes when the data is included during training.</li>
</ol>

<p>By comparing the target query’s score against these two tightly calibrated distributions (Imitative IN vs. Imitative OUT), we can perform highly accurate inference using far fewer models.</p>

<p>The implications of this shift from “shadowing” to “imitation” are profound. 
We found that IMIA achieves superior attack performance while requiring less than 5% of the computational cost of state-of-the-art baselines. 
This reduces attack time from days to hours. 
This performance superiority is consistent across different attack settings (both adaptive and non-adaptive, as introduced in our previous paper<d-cite key="ndss26cpmia"></d-cite>), as well as across different model architectures and benchmarks.</p>

<h2 id="conclusion">Conclusion</h2>

<p>We believe that Imitative Training represents a paradigm shift for MIAs. By moving away from generic shadow training and towards target-informed imitation, we can assess privacy risks more accurately and with a fraction of the computation required by previous approaches.</p>

<p>This shift is especially important for foundation models such as large language models (LLMs).
Some studies argue that current MIAs against LLMs are ineffective. 
However, this may not be because LLMs are resilient to privacy attacks, but simply because existing attacks are too weak or computationally expensive to run at the necessary scale. 
We hope our approach provides a viable solution for this direction. 
There are enormous opportunities to explore along this path, and we invite more researchers to join us in developing powerful, efficient privacy auditing approaches for trustworthy ML.</p>]]></content><author><name>Yuntao Du</name></author><summary type="html"><![CDATA[This is the gentle introduction to our work, which will be presented at USENIX Security 2026. Code and datasets are available on Github.]]></summary></entry><entry><title type="html">Joint membership inference for machine learning models</title><link href="https://yuntaodu.me/blog/2025/CPMIA/" rel="alternate" type="text/html" title="Joint membership inference for machine learning models" /><published>2025-10-01T00:00:00+00:00</published><updated>2025-10-01T00:00:00+00:00</updated><id>https://yuntaodu.me/blog/2025/CPMIA</id><content type="html" xml:base="https://yuntaodu.me/blog/2025/CPMIA/"><![CDATA[<p>This is the gentle introduction to our <a href="http://arxiv.org/abs/2507.21412">work</a>, which will be presented at <a href="https://www.ndss-symposium.org/ndss2026/">NDSS 2026</a>. Code and datasets are available on <a href="https://github.com/zealscott/CPMIA">Github</a>.</p>

<h2 id="introduction">Introduction</h2>

<p>Membership inference attacks (MIAs) have become a standard way to evaluate the empirical privacy risks of machine learning models. The first modern MIA was introduced in 2017<d-cite key="sp17membership"></d-cite>, together with the concept of shadow training. 
Over the past several years, many MIAs have been proposed for different model classes, e.g., image classifiers, GANs, diffusion models, and large language models, to name a few.</p>

<p>All existing MIAs follow a one-by-one procedure: for each membership query instance, the adversary collects an attack signal (from black-box queries or white-box gradients) and outputs a membership prediction for that single instance. 
At first glance, this approach appears reasonable, as the data instances in the training set could be assumed to be independently and identically distributed (i.i.d.), implying that the membership indicators Mi and Mj are marginally independent. 
However, this independence assumption is <strong>violated</strong> when conditioning on the model’s output over the entire memberhsip query dataset. In this conditional setting, the membership indicators, although independent marginally, become dependent due to the collider effect induced by the model’s output, as shown below.</p>

<div class="row justify-content-md-center">
    <div class="col-sm mt-0 pb-0 mt-md-0">
 <figure>

  <picture>
    
    <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/2025-10-01-CPMIA/dependence-480.webp" />
    <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/2025-10-01-CPMIA/dependence-800.webp" />
    <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/2025-10-01-CPMIA/dependence-1400.webp" />
    

    <!-- Fallback to the original file -->
    <img src="/assets/img/2025-10-01-CPMIA/dependence.png" class="img-fluid rounded z-depth-1" width="30%" height="auto" title="Statistical dependencies between query instances when conditional on model's output" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />

  </picture>

</figure>

    </div>
</div>
<div class="caption">
 Statistical dependencies between query instances when conditional on the model's output
</div>

<p>This intriguing observation sparked a key question for us: can we design a more powerful MIA by exploiting this inherent dependence between instances?</p>

<h2 id="a-new-formulation-for-mia">A new formulation for MIA</h2>

<p>To achieve this, we first re-examined the standard security game for MIAs. We found that the existing security game<d-cite key="csf18privacy"></d-cite> fails to distinguish a crucial detail: whether the attacker is allowed to train new shadow models using the query instances themselves. This distinction is critical, as it can lead to a significant difference in attack performance. Furthermore, we noticed a disconnect between this formal definition and common experimental practice, where attacks often process a whole set of query instances at once, not just a single one.
To bridge this gap and clarify when it’s possible to leverage joint information, we reformulated the MIA threat model into two distinct scenarios: <strong>adaptive</strong> and <strong>non-adaptive</strong>.</p>

<p>In the adaptive setting, the adversary receives the full query set and is then allowed to train new shadow models. This enables the attacker to leverage information about both <em>in</em> distribution (member) and <em>out</em> distribution (non-member) behaviors specific to the query set.</p>

<p>It’s worth noting that some studies<d-cite key="sp22lira"></d-cite> refer to these settings as “offline” and “online,” respectively. We find this terminology potentially confusing, as their “offline” setting (no new training per query) is conceptually similar to “online algorithms” that process data sequentially. We therefore advocate using adaptive / non-adaptive to reduce ambiguity.</p>

<h2 id="cascading-membership-inference-attack-cmia">Cascading Membership Inference Attack (CMIA)</h2>

<p>Our first attack, the Cascading Membership Inference Attack (CMIA), is an attack-agnostic framework designed to exploit this membership dependence for a more powerful attack. However, capturing this dependence is not easy. By definition, the attacker doesn’t know the true membership of any query instance to begin with. We borrow the intuition behind Gibbs sampling: estimate a joint distribution from marginal distributions by iterative conditional updates. In our setting, an existing MIA provides approximate marginal membership probabilities for each instance. CMIA then iteratively refines membership predictions by conditioning on increasingly confident labels. Concretely, each cascading iteration works as follows:</p>

<ol>
  <li>Run the base MIA to obtain marginal membership scores for all query instances.</li>
  <li>Fix the most confident predictions as pseudo membership labels.</li>
  <li>Train <em>conditional</em> shadow models using those pseudo-labels to implicitly capture joint behaviors.</li>
  <li>Repeat the process.</li>
</ol>

<p>CMIA can plug into any shadow-based MIA to boost its performance. Unlike full Gibbs sampling (which may require many iterations to converge to the joint distribution), CMIA tends to reach strong performance within just a few cascading steps.</p>

<h2 id="proxy-membership-inference-attack-pmia">Proxy Membership Inference Attack (PMIA)</h2>

<p>CMIA is powerful but relies on the adaptive setting, since it trains shadow models after seeing the query set. That makes it inapplicable in the non-adaptive setting, where the attacker cannot retrain on the queries.</p>

<p>To address non-adaptive scenarios, we formalize the problem as Marginal MIA and derive the theoretically optimal attack strategy. Interestingly, we found that this theoretically optimal attack, which is based on a likelihood ratio test, provides a theoretical justification for why MIAs should focus on achieving a high True Positive Rate (TPR) at a very low False Positive Rate (FPR).</p>

<p>But how do we approximate the <em>in</em> behavior for a query instance if we are not allowed to train shadow models on that instance? We propose a pragmatic solution: use <strong>proxy instances</strong>.</p>

<p>An adversary can train shadow models before observing the query set; those shadow models have <em>in</em> behaviors for whatever data they trained on. By finding training examples (from the adversary’s available data) that are similar to a query instance, we can use those examples’ behaviors as proxies for the query instance’s <em>in</em> distribution. We design proxy-selection strategies at three granularities (global, class, and instance-level) and use them as proxies to construct the likelihood ratio test for attack.
Despite the simplicity of this idea, PMIA performs surprisingly well and often outperforms existing non-adaptive methods by a substantial margin.</p>

<h2 id="conclusion-a-new-frontier-for-mia">Conclusion: A new frontier for MIA</h2>

<p>Our work opens a new avenue for improving membership inference by explicitly modeling dependencies between instances. We have only scratched the surface, yet the initial gains are significant. I believe there is substantial follow-up work to be done, and that exploring joint inference will reshape how MIAs (and their defenses) are designed.</p>

<p>However, this increased power doesn’t come for free. CMIA, for instance, works by iteratively running the base attack. If the base attack is already computationally expensive (like LiRA<d-cite key="sp22lira"></d-cite>), CMIA’s overhead can be substantial. 
Designing efficient but powerful joint-membership attacks is an important and open research question.</p>]]></content><author><name>Yuntao Du</name></author><summary type="html"><![CDATA[This is the gentle introduction to our work, which will be presented at NDSS 2026. Code and datasets are available on Github.]]></summary></entry><entry><title type="html">How to (principally) assess tabular data synthesis algorithms?</title><link href="https://yuntaodu.me/blog/2025/SynMeter/" rel="alternate" type="text/html" title="How to (principally) assess tabular data synthesis algorithms?" /><published>2025-08-25T00:00:00+00:00</published><updated>2025-08-25T00:00:00+00:00</updated><id>https://yuntaodu.me/blog/2025/SynMeter</id><content type="html" xml:base="https://yuntaodu.me/blog/2025/SynMeter/"><![CDATA[<p>This is the gentle introduction to our <a href="https://arxiv.org/abs/2402.06806">work</a>, which will be presented at <a href="https://www.sigsac.org/ccs/CCS2025/">CCS 2025</a>. Code and datasets are available on <a href="https://github.com/zealscott/SynMeter">Github</a>.</p>

<h2 id="introduction">Introduction</h2>

<p>Tabular data is one of the most widely used data structures across science, industry, and governance. As data-driven decision-making becomes the norm, sharing and publishing tabular data is increasingly important. However, sharing data raises serious privacy concerns. A promising solution is <em>data synthesis</em>, which generates synthetic data that mimics the real dataset while preserving privacy. Tabular data synthesis has been studied for years, with algorithms ranging from simple histograms to statistical models (e.g., probabilistic graphical models) to advanced deep learning methods (e.g., diffusion models and large language models). Broadly, synthesis algorithms can be divided into two categories:</p>

<ul>
  <li><strong>Differentially private (DP) synthesizers.</strong> These methods aim to satisfy the rigorous guarantees of differential privacy (DP), providing a provable privacy guarantee. This area is well studied in data privacy and databases. Notable examples include PGM<d-cite key="icml19pgm"></d-cite> and PrivSyn<d-cite key="usenix21privsyn"></d-cite>. Such methods typically add DP noise to selected marginal distributions of the data, then reconstruct a joint distribution to generate synthetic samples. Recent work also explores adding DP noise to gradients (e.g., DP-SGD) to harness deep generative models while preserving DP guarantees.</li>
  <li><strong>Heuristically Private (HP) synthesizers.</strong> These methods do <strong>not</strong> satisfy DP. Instead, they aim to protect privacy <strong>empirically</strong> and are mostly proposed by the machine learning community. The most widely used example is CTGAN<d-cite key="nips19ctgan"></d-cite>, with recent improvements using diffusion models such as TabDDPM<d-cite key="icml23tabddpm"></d-cite>. Since HP synthesizers lack formal privacy guarantees, reliable privacy evaluation metrics become critical for assessing their risks.</li>
</ul>

<h2 id="proposed-evaluation-process">Proposed Evaluation Process</h2>

<p>While so many synthesizers have been proposed, a comprehensive understanding of the strengths and weaknesses of different synthesizers remains elusive. DP synthesizers are rarely compared directly with HP ones, and there’s no standardized way to compare statistical methods against deep generative models. What’s more concerning is that different methods use different evaluation pipelines, making comparisons inconsistent. To address this, we propose a comprehensive evaluation process that works for both DP and HP synthesizers:</p>

<ol>
  <li><em>Data preparation phase</em>. Tabular data usually requires preprocessing before synthesis. Different algorithms use different preprocessing techniques. For instance, statistical methods select low-dimensional marginals to represent distributions compactly, while deep generative models rely on encoding and normalization.</li>
  <li><em>Model tuning</em>. Hyperparameter tuning is crucial for fair comparison but is often poorly documented. We found that proper tuning dramatically affects synthetic data quality. Our paper proposes a simple but effective tuning approach to ensure fair evaluation across different algorithms.</li>
  <li><em>Model training</em>. After tuning, models are trained to generate synthetic data. Some statistical synthesizers<d-cite key="usenix21privsyn"></d-cite> don’t have learnable parameters and may skip this phase, while deep models optimize complex objectives.</li>
  <li><em>Model evaluation</em>. The trained model generates synthetic data, which is then evaluated on three key aspects: <strong>fidelity</strong>, <strong>privacy</strong>, and <strong>utility</strong>.</li>
</ol>

<h2 id="what-makes-a-good-evaluation-metric">What Makes a Good Evaluation Metric?</h2>

<p>It is known that evaluating generative models is inherently difficult, and qualitative evaluation of tabular data through visual inspection is also infeasible.
There are various evaluation metrics proposed, and different methods use distinct evaluation metrics, and they all claim that they are state-of-the-art.
For example, many DP papers rely only on simple one/two-way query errors, while HP methods borrow metrics from image and text generation<d-cite key="iclr16evalgen"></d-cite> to measure fidelity. Even worse, some HP papers use similarity-based privacy metrics that do not align with the principles of differential privacy, leading to misleading conclusions about their privacy risks.</p>

<h3 id="fidelity-evaluation-metrics">Fidelity Evaluation Metrics</h3>

<p>Fidelity measures how closely synthetic data matches real data.
Unlike images or text, where joint distributions are natural to evaluate, tabular data is often better measured through <em>marginal</em> distributions. However, current approaches use different statistical metrics for discrete and continuous attributes, making results hard to compare.</p>

<p>We propose using <strong>Wasserstein distance</strong> to measure marginal similarity. It works consistently for discrete, continuous, or mixed attributes and provides a meaningful structural similarity metric. Interestingly, we found that under this unified metric, some newer DP synthesizers actually underperform compared to simpler statistical methods. However, without DP constraints, advanced models, especially diffusion-based ones, can achieve impressive fidelity and generate highly realistic data.</p>

<h3 id="privacy-evaluation-metrics">Privacy Evaluation Metrics</h3>

<p>We first observe that widely-used privacy evaluation metrics for HP synthesizers (e.g., Distance to Closest Record (DCR)<d-cite key="acml21ctgabgan"></d-cite>) are <strong>syntactic</strong>. That is, they are properties of the input/output dataset pair and are independent of the synthesis algorithm itself. Over the last decade, the privacy community has realized the limitations of such syntactic metrics (like k-anonymity) and has shifted towards semantic privacy definitions like Differential Privacy. Therefore, HP synthesizers evaluated with these syntactic metrics may provide a false sense of privacy. Furthermore, the widely used empirical privacy evaluation approach, i.e., membership inference attack (MIA)<d-cite key="sp17membership"></d-cite>, is not always effective for all types of tabular data synthesis.</p>

<p>Therefore, we propose a new semantic privacy evaluation metric, based on the principles of differential privacy, that works for all types of synthesizers. Unlike MIA, which measures privacy risk from an adversary’s perspective, our approach directly approximates the leave-one-out distinguishability for all records in the training set and utilizes the maximum leakage as the privacy risk measurement.  We present extensive experiments showing that our metric, the Membership Disclosure Score (MDS), can faithfully identify the privacy risks of HP synthesizers and is more effective than MIA and other heuristic privacy metrics.</p>

<h3 id="utility-evaluation-metrics">Utility Evaluation Metrics</h3>

<p>Synthetic data is often used to train models for downstream tasks, such as machine learning prediction. However, unlike vision or NLP, where standardized benchmark models exist, the performance of tabular prediction can vary widely across different models. There’s still ongoing debate<d-cite key="nips23tabular"></d-cite> about whether deep learning even excels in tabular tasks. To mitigate this, we propose measuring the relative performance drop across several ML models when trained on synthetic versus real data. This relative drop serves as our measure of utility preservation.</p>

<h3 id="model-tuning">Model Tuning</h3>

<p>We also find that many synthesis algorithms are highly sensitive to their hyperparameters. Achieving optimal performance for each dataset requires careful tuning. However, most synthesizers do not provide a clear methodology for this tuning process. Therefore, we propose a unified model tuning objective that combines our proposed fidelity and utility metrics. We found that this tuning objective can significantly improve the performance of many synthesizers (especially deep learning-based ones) and enables fairer comparisons. We note that our tuning objective is by no means perfect and advocate for researchers to design specific tuning strategies tailored to their own synthesis algorithms.</p>

<h2 id="discussion--conclusion">Discussion &amp; Conclusion</h2>

<p>In this work, we carefully examine the limitations of existing evaluation metrics for tabular data synthesis and propose a systematic and principled framework for assessing the performance of both DP and HP synthesizers. We hope that future work on tabular data synthesis will adopt our proposed evaluation framework to enable better and fairer comparisons, helping to clearly identify the advantages and limitations of new advances in the field.</p>

<p>However, while our paper makes a significant step toward a standardized evaluation and serves as a bridge to connect and compare DP and HP synthesis algorithms, it has its own limitations. For example, the computation of our privacy metric (MDS) is quite expensive, as it requires training many shadow models for an accurate privacy estimation. Nevertheless, we hope our work will inspire future research to design tabular data synthesis algorithms that achieve both high fidelity and strong privacy.</p>]]></content><author><name>Yuntao Du</name></author><summary type="html"><![CDATA[This is the gentle introduction to our work, which will be presented at CCS 2025. Code and datasets are available on Github.]]></summary></entry><entry><title type="html">Trajectory synthesis with local differential privacy</title><link href="https://yuntaodu.me/blog/2023/LDPTrace/" rel="alternate" type="text/html" title="Trajectory synthesis with local differential privacy" /><published>2023-04-22T00:00:00+00:00</published><updated>2023-04-22T00:00:00+00:00</updated><id>https://yuntaodu.me/blog/2023/LDPTrace</id><content type="html" xml:base="https://yuntaodu.me/blog/2023/LDPTrace/"><![CDATA[<p>This is the gentle introduction to our <a href="https://arxiv.org/abs/2302.06180">work</a> which will be represented at <a href="https://vldb.org/2023/">VLDB 2023</a>. Code and datasets are available at <a href="https://github.com/zealscott/LDPTrace">Github</a>.</p>

<h2 id="introduction">Introduction</h2>

<p>Trajectory data is widely used for many real-world applications, such as tracking the spread of the disease through people’s movement patterns and providing personalized location-based services based on travel preference. However, privacy concerns and data protection regulations have limited the extent to which this data is shared and utilized. Current <a href="https://en.wikipedia.org/wiki/Differential_privacy">differential privacy (DP)</a> techniques rely on a <em>trustworthy aggregator</em> to collect users’ raw trajectories, which still suffer from the risk of data breaches from untrustworthy data curators.</p>

<div class="row justify-content-md-center">
    <div class="col-sm mt-0 pb-0 mt-md-0">
        <figure>

  <picture>
    
    <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/2023_04_22_LDPTrace/dp_and_ldp-480.webp" />
    <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/2023_04_22_LDPTrace/dp_and_ldp-800.webp" />
    <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/2023_04_22_LDPTrace/dp_and_ldp-1400.webp" />
    

    <!-- Fallback to the original file -->
    <img src="/assets/img/2023_04_22_LDPTrace/dp_and_ldp.png" class="img-fluid rounded z-depth-1" width="70%" height="auto" title="DP vs LDP" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />

  </picture>

</figure>

    </div>
</div>
<div class="caption">
    DP vs LDP. Image source: <a href="https://blog.openmined.org/basics-local-differential-privacy-vs-global-differential-privacy/">OpenMinded</a>.
</div>

<p>In contrast, <a href="https://en.wikipedia.org/wiki/Local_differential_privacy">local differential privacy (LDP)</a> allows users to directly share a noisy version of their data, provides a more practical setting and improved privacy properties. The only solution that meets the rigorous privacy requirements is NGRAM<d-cite key="ngram"></d-cite>. However, this approach suffers from poor global utility, auxiliary knowledge dependence, high computational overhead, and vulnerability to attacks. Therefore, we offer a new privacy-preserving paradigm to protect users’ trajectory data, named LDPTrace, to solve these issues while provides the provable guarantee of LDP.</p>

<h2 id="our-approach">Our approach</h2>

<h3 id="overview">Overview</h3>

<p>Instead of perturbing each trajectory individually, we aim to extract the <strong>key movement patterns</strong> of each user and use them to synthesize privacy-preserving and realistic trajectories. Specifically, LDPTrace approaches trajectory synthesis as a generative process, constructing a probabilistic model based on users’ transition records to estimate the global moving patterns. The key movement patterns are locally extracted from raw trajectory and perturbed/uploaded with OUE<d-cite key="oue"></d-cite> protocol. The overall framework is illustrated as below:</p>

<div class="row justify-content-md-center">
    <div class="col-sm mt-0 pb-0 mt-md-0">
        <figure>

  <picture>
    
    <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/2023_04_22_LDPTrace/framework-480.webp" />
    <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/2023_04_22_LDPTrace/framework-800.webp" />
    <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/2023_04_22_LDPTrace/framework-1400.webp" />
    

    <!-- Fallback to the original file -->
    <img src="/assets/img/2023_04_22_LDPTrace/framework.png" class="img-fluid rounded z-depth-1" width="70%" height="auto" title="Framework" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />

  </picture>

</figure>

    </div>
</div>
<div class="caption">
    The framework of LDPTrace.
</div>

<h3 id="key-movement-patterns">Key movement patterns</h3>

<p>The probabilistic model consists of three key features:</p>

<ol>
  <li><strong>Intra-Trajectory Mobility</strong>. Transition states are important to capture the movement of spatial data, and we model the mobility with a first-order Markov chain, which means we only care the spatial relationship between adjacent points.</li>
</ol>

<p>\begin{equation}
\operatorname{Pr}(T[l+1]=C \mid T[1] \ldots T[l])=\operatorname{Pr}(T[l+1]=C \mid T[l])
\end{equation}</p>

<ol>
  <li>
    <p><strong>Beginning/Terminated Transitions</strong>. Real-world trajectory usually exhibit special start point and end point, which reveal the important spatial semantic of trajectories: pickups, home/work places, destinations, etc. Besides, the start/end points are also useful to guide the random walk during synthesis. Therefore, we add two special cells, namely <em>virtual</em> start point and <em>virtual</em> end point, which are connected to all the geographic points and are perturbed and uploaded along with the mobility patterns.</p>
  </li>
  <li>
    <p><strong>Trajectory Length</strong>. The length distribution of trajectories is also an indispensable character of trajectory synthesis since it indicates the probabilistic travel distance of users’ trace, and serves as the deterministic constraint to terminate the synthesis process. We also use OUE to gather length information from each trajectory and form a discrete distribution on the data curator.</p>
  </li>
</ol>

<h3 id="trajectory-synthesis">Trajectory synthesis</h3>

<p>After getting the three global spatial features from trajectories, we first combine the intra-trajectory mobility with virtual start/end mobility, and utilize random walk to generate authentic trajectories from them. Here are the main steps of our synthesis algorithm:</p>

<div class="row justify-content-md-center">
    <div class="col-sm mt-0 pb-0 mt-md-0">
        <figure>

  <picture>
    
    <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/2023_04_22_LDPTrace/synthesis-480.webp" />
    <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/2023_04_22_LDPTrace/synthesis-800.webp" />
    <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/2023_04_22_LDPTrace/synthesis-1400.webp" />
    

    <!-- Fallback to the original file -->
    <img src="/assets/img/2023_04_22_LDPTrace/synthesis.png" class="img-fluid rounded z-depth-1" width="60%" height="auto" title="Trajectory synthesis" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />

  </picture>

</figure>

    </div>
</div>
<div class="caption">
    Trajectory synthesis from key movement patterns.
</div>

<p>It is worth mentioning that we found the adaptive terminated condition (line 5) is very helpful to generate faithful trajectories (otherwise it would be too short to show the spatial characters), but it is still an open question that how to integrate the handcraft adaptive mechanism into the feature selection process.</p>

<h2 id="experiments">Experiments</h2>

<h3 id="effectiveness">Effectiveness</h3>

<p>We conduct extensive experiments on four benchmark trajectory datasets (three of them are released in our <a href="https://github.com/zealscott/LDPTrace">repository</a>). For utility metrics, we follow AdaTrace<d-cite key="adatrace"></d-cite> and comprehensively evaluate our method with various metrics from global level, trajectory level to semantic level. From fair comparison, we discard the auxiliary temporal and POI information in NGRAM, and only leverage physical distance to ensure the closeness between original and sampled n-grams in geospatial space. The results show that LDPTrace outperform the baseline by a large margin.</p>

<div class="row justify-content-md-center">
    <div class="col-sm mt-0 pb-0 mt-md-0">
        <figure>

  <picture>
    
    <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/2023_04_22_LDPTrace/utility-480.webp" />
    <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/2023_04_22_LDPTrace/utility-800.webp" />
    <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/2023_04_22_LDPTrace/utility-1400.webp" />
    

    <!-- Fallback to the original file -->
    <img src="/assets/img/2023_04_22_LDPTrace/utility.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" title="Utility performance comparison" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />

  </picture>

</figure>

    </div>
</div>
<div class="caption">
    Utility performance comparison. The best result in each category is shown in bold. For Kendall-tau and FP F1 Similarity, higher values are better. For remaining metrics, lower values are better.
</div>

<h3 id="efficiency">Efficiency</h3>

<p>Efficiency is equally important as utility for real-world deployments, we conduct comprehensive experiments to evaluate
the average running time of each component, as show in the following figure. We find that LDPTrace is a very efficient privacy-preserving trajectory publication framework, which is more than 300 times faster than NGRAM.</p>

<div class="row justify-content-md-center">
    <div class="col-sm mt-0 pb-0 mt-md-0">
        <figure>

  <picture>
    
    <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/2023_04_22_LDPTrace/efficiency-480.webp" />
    <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/2023_04_22_LDPTrace/efficiency-800.webp" />
    <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/2023_04_22_LDPTrace/efficiency-1400.webp" />
    

    <!-- Fallback to the original file -->
    <img src="/assets/img/2023_04_22_LDPTrace/efficiency.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" title="Runtime" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />

  </picture>

</figure>

    </div>
</div>
<div class="caption">
    Average runtime in seconds.
</div>

<h3 id="attack-resilience">Attack resilience</h3>

<p>We also investigate LDPTrace’s resistance to two common attacks, namely re-identification attack and outlier attack, which are defined in this paper<d-cite key="adatrace"></d-cite>. The results indicate that LDPTrace has impressive ability to resist these two attacks: more than 88% trajectories can be successfully protected in re-identification attack, and all trajectories are hidden from outliers when the privacy parameter changes its value from 2 to 10. On the contrary, NGRAM cannot provide provable protections to these attacks, especially when the demand of protection is more strict. We contribute the superiority of LDPTrace to the synthesis design: since the published trajectories do not resemble any real trajectory, it is much more difficult for attackers to identify traces that they are interested in.</p>

<div class="row justify-content-md-center">
    <div class="col-sm mt-0 pb-0 mt-md-0">
        <figure>

  <picture>
    
    <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/assets/img/2023_04_22_LDPTrace/attack-480.webp" />
    <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/assets/img/2023_04_22_LDPTrace/attack-800.webp" />
    <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/2023_04_22_LDPTrace/attack-1400.webp" />
    

    <!-- Fallback to the original file -->
    <img src="/assets/img/2023_04_22_LDPTrace/attack.png" class="img-fluid rounded z-depth-1" width="70%" height="auto" title="Attack" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />

  </picture>

</figure>

    </div>
</div>
<div class="caption">
    Attack resilience analysis on Oldenburg dataset.
</div>

<h3 id="ablation-study">Ablation study</h3>

<p>Finally, we conduct in-depth analysis with LDPTrace, like impact of beginning/terminated transitions, impact of grid granularity, impact of different query size. Please refer to our paper for details if interested.</p>

<h2 id="conclusion">Conclusion</h2>

<p>In this paper, we present a neat yet effective trajectory synthesis
framework under the rigorous privacy of LDP, called LDPTrace,
which achieves strong utility and efficiency simultaneously. Although the privacy issues of trajectory data is extensively studied in recent year, I still think there is more room to achieve better privacy-utility-efficiency tradeoff. Maybe the synthesis approach (LDPTrace) is a plausible way.</p>]]></content><author><name>Yuntao Du</name></author><summary type="html"><![CDATA[This is the gentle introduction to our work which will be represented at VLDB 2023. Code and datasets are available at Github.]]></summary></entry></feed>