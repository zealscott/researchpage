# TODO

AI agents: read this file at the start of every session and remind Yuntao of
any open items before doing other work.

- [ ] Upload the paper URL (e.g. arXiv) for "Privacy Leakage from a Thousand
      Words: Sub-Pixel Location Recovery from Dot Maps" (CCS 2026), then add it
      to the `ccs26_dotmap` entry in `_bibliography/papers.bib` (as
      `arxiv = {...}` or `html = {...}`) and link it in the CCS 2026 news item
      (`_news/announcement_5.md`).
- [ ] After the URL exists, replace the old "location data" link
      (arXiv 2302.06180, LDPTrace) in the homepage Data Privacy pillar
      (`_pages/about.md`) with this new dot-map paper's link.
